name: dashboard
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0-266.0.dev <4.0.0'

dependencies:
  fl_chart: ^0.61.0
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/images/
    - assets/images/logo.png
    - assets/images/rayeneben.png
    - assets/images/blackman.png
    - assets/icons/
